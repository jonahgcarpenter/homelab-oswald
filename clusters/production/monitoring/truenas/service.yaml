apiVersion: v1
kind: Service
metadata:
  name: truenas-exporter
  labels:
    app: truenas-exporter
spec:
  type: ClusterIP
  selector:
    app: truenas-exporter
  ports:
    - name: metrics
      port: 9100
      targetPort: metrics
      protocol: TCP
